export default function TravelProductItem(data) {
  const {
    idx,
    name,
    mainImage,
    description,
    spaceCategory,
    price,
    maximumPurchases,
    registrationDate,
  } = data;
  return <div></div>;
}
